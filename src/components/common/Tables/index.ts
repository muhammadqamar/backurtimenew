import { DataTable } from "./Table";

import {
  TableRowType,
  ColumnConfig,
  CellData,
  RowData,
  TableData,
} from "./types";

export { DataTable };
export type { TableRowType, ColumnConfig, CellData, RowData, TableData };
